{
  "name": "o2o-metabase-viz",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present",
    "prettier": "prettier --write .",
    "build:one": "npm run build -w"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
